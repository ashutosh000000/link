
#include <stdio.h>
#include<malloc.h>
struct node{
    int data;
    struct node *next;
};
void append(struct node **,int);
void display(struct node *);
void delete(struct node *,struct node **h,int);
int main()
{
    
    int data;
    struct node *head=NULL;
    int v;
    
    do{
        printf("Enter the student roll number:\n");
        scanf("%d",&data);
        if(data!=-1)
        append(&head,data);
    }while(data!=-1);
    printf("The students are seated in the order:\n");
    display(head);
    
    printf("\nThe number of students needed for selection");
    scanf("%d",&v);
    
    delete(head,&head,v);
        printf("\nThe student in class after sending for selection is:\n");
        display(head);
    
    return 0;
}
void append(struct node **headadd,int data){
    struct node *temp,*newnode;
    temp=*headadd;
    newnode=(struct node*)malloc(sizeof(struct node*));
    newnode->data=data;
    newnode->next=NULL;
    if(*headadd==NULL){
        *headadd=newnode; 
    }else{
        while(temp->next!=NULL){
            temp=temp->next;
        }
        temp->next=newnode;
    }
}
void display(struct node *headdis){
    while(headdis!=NULL){
        printf("%d ",headdis->data);
        headdis=headdis->next;    
        }
        
}
void delete(struct node *h,struct node **h2,int v){
    int count=0;
    struct node *temp,*temp1;
        temp=h;
        temp1=h;
        while(h!=NULL){
            if(h->data==v)
            break;
            temp=h;
            h=h->next;
        }
        if(temp==h){
            temp1=h->next;
            *h2=NULL;
            *h2=temp1;
        }else{
        temp1=h->next;
        temp->next=NULL;
        temp->next=temp1;
        }
}
